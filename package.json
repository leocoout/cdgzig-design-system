{
  "name": "cdgzig-design-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build",
    "generate": "style-dictionary build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "style-dictionary": "^3.9.0"
  },
  "devDependencies": {
    "token-transformer": "^0.0.33"
  }
}
